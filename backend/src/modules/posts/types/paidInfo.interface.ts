export interface PaidInfoInterface {
  countViews: number; // всі перегляди
  averagePrise: number; // середня ціна
  viewsByDay: number; // за день
  viewsByWeek: number; // за день
  viewsByMonth: number; // за день
}
